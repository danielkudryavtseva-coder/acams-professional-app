export default async function ResetPasswordStubPage() {
  return (
    <div className="mx-auto w-full max-w-xl space-y-4">
      <h1 className="text-3xl font-semibold tracking-tight text-ink">Reset password</h1>
      <p className="leading-relaxed text-ink/75">
        Tokenized reset journeys are staged for a later sprint. Until then executives can regenerate access through the invite tooling (single-use hashed tokens + SMTP
        logging in dev).
      </p>
    </div>
  );
}
