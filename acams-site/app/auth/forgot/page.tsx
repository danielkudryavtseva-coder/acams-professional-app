export default async function ForgotPasswordStubPage() {
  return (
    <div className="mx-auto w-full max-w-xl space-y-4">
      <h1 className="text-3xl font-semibold tracking-tight text-ink">Forgot password</h1>
      <p className="leading-relaxed text-ink/75">
        Password reset hasn&apos;t been wired yet — see the TODO backlog. For urgent access ask an exec to revoke + re-invite you, or authenticate using the nodemailer
        provider if your account permits it.
      </p>
    </div>
  );
}
