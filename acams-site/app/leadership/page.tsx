export default async function LeadershipPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-4xl font-semibold tracking-tight text-ink">Leadership</h1>
        <p className="mt-4 max-w-3xl text-pretty leading-relaxed text-ink/75">
          Crimson leadership bios, timelines, programming lanes — hydrate this route from the SPA MDX/mockups when copy stabilizes.
        </p>
      </div>
      <div className="rounded-2xl border border-dashed border-ink/20 bg-paper p-16 text-center text-sm font-semibold text-ink/60">
        Content placeholder — SPA remains canonical until ported.
      </div>
    </div>
  );
}
