export default async function PortfolioPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-4xl font-semibold tracking-tight text-ink">Portfolio</h1>
        <p className="mt-4 max-w-3xl text-pretty leading-relaxed text-ink/75">
          This mirrors the SPA&apos;s holdings narrative; wire it to identical JSON sources when you unify data layers. Charts stay in the heavier Vite build for bundle
          size reasons — this placeholder keeps navigation coherent while auth ships independently.
        </p>
      </div>
      <section className="rounded-2xl border border-dashed border-ink/20 bg-paper p-16 text-center text-sm font-semibold text-ink/60">
        Embed `professional-app` Portfolio microapps here when ready — authentication already flows downstream.
      </section>
    </div>
  );
}
