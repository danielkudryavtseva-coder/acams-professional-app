/**
 * Utility re-export for CSV parsing (mirrors src/app/data/csvParser.ts for compatibility).
 */
export { parseCSV, csvToContacts } from "../data/csvParser";
export type { ParseResult } from "../data/csvParser";
